#pragma once
#include "../lists/array-list.h"
#include "../lists/linked-list.h"
#include <vector>

void insertionSort(vector<DateTime>& vec);

void insertionSort(Linked_List<DateTime> list);

void insertionSort(Array_List<DateTime> arr_list);